import { AverageMarksPipe } from './average-marks.pipe';

describe('AverageMarksPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageMarksPipe();
    expect(pipe).toBeTruthy();
  });
});
