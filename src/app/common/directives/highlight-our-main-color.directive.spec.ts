import { HighlightOurMainColorDirective } from './highlight-our-main-color.directive';

describe('HighlightOurMainColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightOurMainColorDirective();
    expect(directive).toBeTruthy();
  });
});
